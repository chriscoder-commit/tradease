class WelcomeController < ApplicationController

  #this page is largely empty of code because the landing page's primary purpose is to render a HTML doc, and does so implicitly
  def landing
  end 
end
