hi <%= current_user.username %>

<%= form_with(scope: :review, url: profile_reviews_path(profile), local: true) do |form| %>
  <%= form.label :body %>
  <%= form.text_field :body %>
  <%= form.label :stars %>
  <%= form.number_field :stars %>
  <%= form.submit id: "submit" %>
<% end %>



